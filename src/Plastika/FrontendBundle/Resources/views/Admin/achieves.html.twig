{% extends 'PlastikaFrontendBundle:Layout:admin.html.twig' %}

{% block title %}Достижения{% endblock %}

{% block body %}
   <div class="container">
      <h2>Достижения</h2>
      <br /><br />
      <a class="btn btn-primary" href="{{path('achievecreate')}}">Добавить достижение</a>
      <br /><br /><br />
      <div class="list-group">
      {% for item in achieves %}
      <a href="{{path('achieveedit', {'id' : item.id})}}" class="list-group-item">
         <span class="badge" onclick="javascript:if (!confirm('Вы уверены?')) { return false; } else { location.href='{{path('achievedelete', {'id' : item.id})}}'; return false; }">x</span>
         {{item.title}}
      </a>
      {% endfor %}
      </div>
      
   </div>
   
{% endblock %}